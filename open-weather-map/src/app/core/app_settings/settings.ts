import { environment } from './../../../environments/environment';

export const settings = {
    cities: '../../../assets/cities.json',
    cityDetails: environment.apiBaseUrl + 'data/2.5/history/'
}